
// Returns length of given string
int strlen(char* str);

// Returns concatenated string
// TODO: support cancatenating strings with more than 1024 bytes
char* strconcat(char* str1, char* str2, unsigned long n, char* arr);
char* strcat(char* s1, char* s2);